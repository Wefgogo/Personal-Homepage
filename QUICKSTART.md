# 🚀 快速开始

## 第一步：安装依赖

在 PowerShell 中运行：

```powershell
cd e:\Projects\VueProjects\PersonalWebsite
npm install
```

## 第二步：启动开发服务器

```powershell
npm run dev
```

浏览器会自动打开 `http://localhost:3000`

## 第三步：自定义内容

### 1️⃣ 修改个人信息

打开 `src/data/index.ts`，修改：
- 姓名、职位、简介
- 社交链接（GitHub、LinkedIn、Email 等）
- 头像路径

### 2️⃣ 添加项目

在 `src/data/index.ts` 的 `projects` 数组中添加你的项目：
- 研究项目（Research）
- 工程项目（Engineering）
- 开源贡献（Open Source）

### 3️⃣ 更新简历

修改 `education` 和 `experience` 数组：
- 教育背景
- 工作/实习经验
- 技能列表

### 4️⃣ 编写博客

在 `blogPosts` 数组中添加文章

### 5️⃣ 替换图片

将占位图片 `https://via.placeholder.com/...` 替换为实际图片路径

## 📁 重要文件

| 文件 | 说明 |
|------|------|
| `src/data/index.ts` | 所有个人数据（重点修改） |
| `src/styles/main.css` | 全局样式和主题颜色 |
| `src/views/*.vue` | 各个页面组件 |
| `public/` | 静态资源（图片、简历 PDF） |

## 🎨 自定义主题颜色

编辑 `src/styles/main.css` 中的 CSS 变量：

```css
:root {
  --primary-color: #667eea;      /* 改成你喜欢的颜色 */
  --secondary-color: #764ba2;
  --accent-color: #3b82f6;
}
```

## 📧 设置联系表单

在 `src/views/Contact.vue` 的 `handleSubmit` 函数中集成：
- [EmailJS](https://www.emailjs.com/) - 推荐
- [Formspree](https://formspree.io/)
- 或自建后端 API

## 🚀 部署到线上

### 方案 1：Vercel（推荐）

```powershell
npm install -g vercel
vercel
```

### 方案 2：Netlify

1. 运行 `npm run build`
2. 上传 `dist` 文件夹到 Netlify

### 方案 3：GitHub Pages

1. 修改 `vite.config.ts`，设置 `base: '/仓库名/'`
2. 运行 `npm run build`
3. 部署 `dist` 文件夹

## ❓ 遇到问题？

查看 `USAGE_GUIDE.md` 获取详细说明

## 🎯 下一步

- ✅ 安装依赖
- ✅ 启动开发服务器
- ⬜ 修改个人信息
- ⬜ 添加项目数据
- ⬜ 更新简历
- ⬜ 替换所有图片
- ⬜ 自定义主题颜色
- ⬜ 部署到线上

祝你好运！🎉
